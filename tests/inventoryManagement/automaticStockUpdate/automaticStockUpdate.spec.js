import { test } from '@playwright/test';

test.describe('Real-time Stock Validation', () =>{

    test.beforeEach(async ({page}) => {
        //TO-DO
    });

    test('Stock is correctly deducted after a successful purchase', async ({ page }) => {
        //TO-DO
    });

    test('Stock is not deducted after a failed checkout', async ({ page }) => {
        //TO-DO
    });

});


