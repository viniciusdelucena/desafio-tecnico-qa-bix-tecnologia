{
  "name": "qa-pl-challenge",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start:dev": "node backend/server.js",
    "cy:open": "cypress open",
    "test": "start-server-and-test start:dev http://localhost:3000 'cypress run'"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.6.2",
    "cypress": "^13.12.0",
    "start-server-and-test": "^2.0.3"
  }
}
